# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# --- Virtual environments ---
.venv/
venv/
env/
ENV/

# --- Streamlit ---
.streamlit/secrets.toml

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# --- Testing / coverage ---
.pytest_cache/
.coverage
htmlcov/

# NOTE: this project intentionally does NOT ignore src/ or data/ —
# both contain files the app loads at runtime (source modules and
# the CSV/JSON/SQLite fixtures under data/sources, data/collected,
# data/briefs). Do not add "data/" or "src/" to this file.
